## 背景

日志系统有很多代码层的定时任务，启停测试以及维护都及其不方便。

```go
// 例如：
AddCronFunc(d.LogCron, v3log_metric.CheckV3SingleLogCronKey, "0 0,5,10,15,20,25,30,35,40,45,50,55 * * * ?", hostName, func() {
	v3log_metric.CheckV3DomainList()
})
```

每日的机器割接消息，还会生成多个一次性任务。

## 期望

将上述定时任务，以带有前端的界面方式管理起来，可以方便动态的启动、停止、以及查看运行记录运行日志。

## 需求

- 一次性任务，运行一次立即过期，At 任务
- 周期任务（cron 表达式控制）

需要支持的任务类型

- http 任务 （定时请求http接口）
- shell 任务 （白名单内的 shell 脚本或可执行程序）
- go func 任务

## 技术栈

- Go; Gin; gorm; MySQL
- 任务调度引擎使用 基于redis的 **Asynq**
- 前端 Vue 3 + Vite + Element Plus
